name: run unit test of feature branch

on:
  push: 
    branches: 
      'feature/*'
jobs:
  run-unit-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: run unit test
        run: mvn test
